// ─────────────────────────────────────────────────────────────────────────────
// CHUNK LAZY — IndexContent
// Contenido real del índice de productos.
// Se monta tras el mount inicial; reemplaza el skeleton.
// TODO: reemplazar datos mock con API real cuando esté disponible.
// ─────────────────────────────────────────────────────────────────────────────

const IndexContent = () => (
  <div className="index__content">
    <p>Product index — placeholder</p>
  </div>
)

export default IndexContent
